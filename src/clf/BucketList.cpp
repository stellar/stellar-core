#include "BucketList.h"

namespace stellar
{
    /* SANITY
	bool BucketList::load(stellarxdr::uint256 ledgerHash)
	{

		return(true);
	}

	void BucketList::calculateHash()
	{

	}
	
	void BucketList::addEntry(stellarxdr::uint256& newHash, SLE::pointer newEntry)
	{

	}
	void BucketList::updateEntry(stellarxdr::uint256& oldHash, stellarxdr::uint256& newHash, SLE::pointer updatedEntry)
	{

	}
	void BucketList::deleteEntry(stellarxdr::uint256& hash)
	{

	}

	// need to call after all the tx have been applied to save that last versions of the ledger entries into the buckets
	void BucketList::closeLedger()
	{

	}

	uint256 BucketList::getHash()
	{
		return(mHash);
	}
    */

}